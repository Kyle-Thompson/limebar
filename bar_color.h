#pragma once

#include "color.h"
#include "config.h"

/** BarColors
 * Collection of all colors relevant to the bar.
 */
struct BarColors {
  rgba_t background;
  FontColor foreground;
  FontColor fg_accent;
};
